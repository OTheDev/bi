# bi
A BigInt implementation in C.

It is far from perfect and incomplete, but this has been a big interest of mine 
ever since transitioning to higher-level languages than C (my first language) 
that have built-in support for arbitrary precision integers.
